overwrite: true
schema: "../news_service/schema.graphql"
documents: "graphql/queries/*.graphql"
generates:
  graphql/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-graphql-request"
